//=====> TP : Manipuler un formulaire<=====
import React from "react";
import ReactDOM from "react-dom/client";
import Inscription from './Controle/Inscription';

const element=document.getElementById('root');
const root=ReactDOM.createRoot(element);
root.render(
  <Inscription />
);


/*=====> TP : JeuDe <=====
import React from "react";
import ReactDOM from "react-dom/client";
import JeuDe from "./TpJeuDe/JeuDe";
const element=document.getElementById('root');
const root=ReactDOM.createRoot(element);
root.render(
  <JeuDe cache={2}/>
)
*/
/*=====> Seance 4 : TP1<=====
import React from "react";
import ReactDOM from "react-dom/client";
import Products from "./TpSeance4P2/Products";

const element=document.getElementById('root');
const root=ReactDOM.createRoot(element);
root.render(
  <Products/>
)
*/
/*=====> Seance 5 : TP2 composant Voiture données à partir d’un objet <=====
import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";

const element=document.getElementById('root');
const root=ReactDOM.createRoot(element);
root.render(
  <App/>
)
*/
/*=====> Seance 5 : TP1 P1 classes composants <=====
//=====> Seance 5 : TP1 P1 fonctionnels composants <=====
//=====>  TP : Incrementer et décrémenter un nombre (props et state) <=====
import React from "react";
import App from './App';
import ReactDOM from "react-dom/client";
const element=document.getElementById("root");
const root=ReactDOM.createRoot(element);
root.render( <App/> )
*/
/*====> Index <====
import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

*/
// If you want to start measuring performance in your app, pass a function
// to log results (for example: reportWebVitals(console.log))
// or send to an analytics endpoint. Learn more: https://bit.ly/CRA-vitals

// reportWebVitals();

